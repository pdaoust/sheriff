{
  "author": "Paul d'Aoust <paul@heliosstudio.ca> (http://heliosstudio.ca)",
  "name": "sheriff",
  "description": "A validator based loosely on JSON Schema, with the ability to throw custom functions into the schema.",
  "version": "0.0.2",
  "main": "index.js",
  "scripts": {
    "test": "jasmine-node tests/"
  },
  "dependencies": {"wrangler": ">= 0.0.2"},
  "devDependencies": {},
  "optionalDependencies": {},
  "engines": {
    "node": "*"
  }
}
